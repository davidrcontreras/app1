There are two files ci.yml:

    - The file called ci.yml will be used

    - One of the files is to perform a CI test on the app using a python module (test_app.py).

    - The other is to test the app in the GitHub infrastructure. 

Rename ci.yml to the file you want to use.